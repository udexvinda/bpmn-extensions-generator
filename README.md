# bpmn-extensions-generator
Custom attributes (like "KPI Target" or "Risk Level") to process elements, which are stored as part of the process's underlying XML data.

https://bpmn-extensions-generator.streamlit.app/
